-Async sockets: gevent
-TCL: pkg Tkinter
-Async DNS: gevent
-SSL: pkg gevent.ssl
-xml-rpc: pkg xmlrpc
-timers: gevent.Timeout(?)

Planned Features:
cmd->rpl tracking

Later:
IPv6
SSL

TODO:
flood protection
Connection queueing
comments
MODE rpl parsing
TOPIC rpl parsing
reconnect queue
support return values for events
	* make sure that handlers call .stop_handlers() when they return something
	* rework authentication_event to use that feature